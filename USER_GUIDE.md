# 📖 知识空间使用手册

> 面向使用者的完整指南：从部署、配置到日常使用、问题排查，照着做就行。

---

## 一、这个工具是干什么的？

**一句话：把你在网上看到的好内容，一键存进 Notion 知识库，AI 自动帮你整理。**

看到一篇好文章、一条视频、一个帖子，不用再手动复制粘贴整理。你只需要：

```
看到好内容 → 分享/收录 → AI 自动提取正文 → AI 生成摘要/分类/标签 → 写入 Notion → 手机收到通知
```

支持两种使用方式：

| 方式 | 场景 | 通知 |
|---|---|---|
| 📱 **手机端**（iOS 快捷指令） | 手机上看到好内容，分享菜单一键收录 | Bark 推送通知 |
| 🌐 **电脑端**（Chrome 插件） | 电脑浏览器看到好内容，点插件收录 | Chrome 系统通知 |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│                 你（手机 / 电脑）                      │
│   iOS 快捷指令          Chrome 插件                  │
└──────────────┬──────────────────┬────────────────────┘
               │ 发送链接          │ 发送链接
               ▼                  ▼
┌─────────────────────────────────────────────────────┐
│              Cloudflare Worker（核心服务）            │
│                                                     │
│  1. 内容提取：TikHub（知乎/小红书/B站/公众号）          │
│      → Firecrawl（通用兜底）                          │
│      → 火山引擎 OCR（小红书图片文字）                   │
│  2. AI 分析：摘要 / 分类 / 标签 / 关键要点 / 实体        │
│  3. 写入 Notion 知识库                                │
│  4. 推送通知：Bark（手机）                            │
│                                                     │
│  配套：D1 数据库（日志+配置）、监控台（查看收录记录）     │
└─────────────────────────────────────────────────────┘
```

---

## 三、部署（只需做一次）

> 详细步骤见 [README.md](README.md) 的「部署指南」。这里只列要点。

### 前置准备

1. **Windows 用户**：安装 Git for Windows，用 **Git Bash** 操作（不要用 PowerShell/CMD）
2. 安装 Node.js 18+
3. 安装 Cloudflare 工具：`npm i -g wrangler`
4. 登录 Cloudflare：`npx wrangler login`（会弹浏览器授权）
5. 确认已注册 workers.dev 子域名

### 一键部署

```bash
# 第 1 步：获取代码（GitHub 项目页下载 ZIP 或 git clone）
# 第 2 步：进入项目目录
# 第 3 步：执行
bash scripts/setup.sh
```

脚本自动完成：建库 → 写配置 → 建队列 → 建表 → 部署 → 生成令牌。

**⚠️ 跑完后务必保存两样东西：**

```
Worker URL: https://xxx.workers.dev
INGEST_TOKEN: <你的令牌>
```

> INGEST_TOKEN 相当于你的"钥匙"，登录配置页、配置手机快捷指令、配置浏览器插件都要用。丢了只能重新部署。

---

## 四、配置（部署后必做）

浏览器打开部署时打印的"请访问"地址，按页面 5 步配置：

| 步骤 | 配置项 | 需要准备 |
|---|---|---|
| 1 | **内容提取** | TikHub Key（必配）、Firecrawl Key（可选）、火山引擎 OCR（可选，抓小红书图片才要） |
| 2 | **AI 分析** | OpenAI 兼容大模型的 API Key（DeepSeek/豆包/通义等） |
| 3 | **写入知识库** | Notion Token + 数据库 ID |
| 4 | **安装客户端** | 按页面指引装 Chrome 插件 + iOS 快捷指令 |
| 5 | **推送通知** | Bark Key（手机端需要） |

### 各服务怎么拿 Key

| 服务 | 用途 | 获取方式 |
|---|---|---|
| **TikHub** | 知乎/小红书/B站/公众号正文提取（必配） | tikhub.io 注册 |
| **Firecrawl** | 通用网页兜底提取（可选） | firecrawl.dev 注册 |
| **火山引擎 OCR** | 小红书图片文字识别（可选） | 火山引擎控制台，**先开通免费的「通用文字识别」服务**，再创建 AK/SK |
| **大模型 API** | AI 摘要/分类/标签 | DeepSeek / 豆包 / 通义等平台 |
| **Notion** | 知识库存储 | notion.so/profile/integrations 创建集成 + 建数据库 |
| **Bark** | iOS 推送 | App Store 下载，首页 `api.day.app/` 后面那串就是 Key |

---

## 五、日常使用

### 📱 手机端：iOS 快捷指令

**收录**：
1. 在任意 App 看到好内容，复制链接（或分享）
2. 点分享按钮 → 点「更多」→ 找到「一键收录」快捷指令
3. 等几秒，完成后手机会收到 Bark 推送通知

**常见问题**：
- **小红书**：平台限制，需先复制链接到微信或浏览器，再分享收录
- **收录超时/请求超时**：通常是网络问题（`workers.dev` 域名在国内部分网络下被墙），开启代理/VPN 重试，或给 Worker 绑定自己的域名

### 🌐 电脑端：Chrome 插件

1. 在网页上看到好内容
2. 点浏览器工具栏的插件图标 → 一键收录当前页面 / 选中文本
3. 完成后 Chrome 弹系统通知提醒

### 🖥️ 监控台

用「请访问」地址打开监控台，可以：
- 查看所有收录记录（成功/失败状态、来源平台、设备、耗时）
- 搜索记录（按 URL/标题/平台）
- 测试各服务的连通性
- 配置页里修改各 Key 和提示词

---

## 六、Notion 知识库字段说明

每个收录的内容会写入 Notion，共 **12 个字段**：

### 系统自动填充（6 个，不用管）

| 字段 | 说明 |
|---|---|
| Source URL | 原文章链接 |
| Captured At | 收录时间 |
| Source Platform | 来源平台（按域名识别：知乎/小红书/B站等） |
| Content Type | 内容类型（文章/视频字幕/图文） |
| Author | 原文作者 |
| Published At | 原文发布时间（抓不到则为空） |

### AI 生成（6 个，可调提示词）

| 字段 | 说明 |
|---|---|
| Title | 最终标题 |
| Summary | 中文摘要（120-350 字） |
| Key Points | 关键要点（2-4 条） |
| Category | 分类（从分类库选） |
| Tags | 标签（3-6 个，从标签池选） |
| Entities | 关键实体（公司/产品/人物/术语） |

> ⚠️ 这 6 个 AI 字段名是**固定的，不能增减**。但字段的内容要求（摘要字数、要点条数、分类选项、标签池等）可以在提示词里自由调整。

---

## 七、自定义 AI 分析（提示词）

配置页 → AI 分析 → 可以修改**系统提示词**和**用户提示词**：

| 提示词 | 管什么 | 举例 |
|---|---|---|
| **系统提示词** | AI 的角色、目标、整体输出约束 | "你是知识整理助手，只输出 JSON" |
| **用户提示词** | 每个字段的具体要求 + 模板 | 摘要 120-350 字、分类从这些选、标签从这些池选 |

**可以调的内容**：
- 摘要字数范围
- 要点条数
- 分类列表选项
- 标签池内容
- 实体识别范围

**不能调的**：字段数量（6 个 AI 字段固定）、Notion 列名（和代码里一一对应）。

---

## 八、常见问题排查

| 问题 | 原因 | 解决办法 |
|---|---|---|
| **手机端收录超时** | `workers.dev` 网络被墙 | 开代理/VPN 重试，或绑定自定义域名 |
| **抓取失败（HTTP 400）** | 链接不是纯链接（混入了文字） | 检查快捷指令的「匹配文本」正则是否填对：`https?://[^\s]+`（注意 `\s` 前的反斜杠不能少） |
| **Notion 写入报错** | Notion 数据库缺列/列名不对 | 检查数据库是否包含全部 12 个字段，列名要英文一致 |
| **AI 没分析** | 没配大模型 API Key | 配置页 → AI 分析 → 填 API Key |
| **收录成功但没通知** | 通知权限没开 | 手机：Bark 配置正确；电脑：macOS 设置 → 通知 → 允许 Chrome |
| **Bark Key 找不到** | 不知道 Key 在哪 | 打开 Bark，首页 `api.day.app/` 后面那串就是 |

---

## 九、更新代码

```bash
cd knowledge-space-worker   # 进入项目目录
git pull                    # 拉取最新代码
bash scripts/setup.sh       # 重新部署（幂等，可重复运行）
```

---

## 十、技术栈速览（给懂技术的人）

- **Cloudflare Workers** — Serverless 运行时
- **Cloudflare D1** — SQLite 数据库（日志和配置）
- **Cloudflare Queues** — 异步任务队列
- **TikHub / Firecrawl / 火山 OCR** — 内容提取
- **OpenAI 兼容 LLM** — AI 分析
- **Notion API** — 知识库
- **Bark** — iOS 推送
- **Chrome Extension** — 电脑端收录入口

---

*手册版本 v1.0 · 2026-08-19*
